# Copy to .env.local for local dev, or set these in your Vercel project settings.

# The "Client Daily Inquiries" workbook ID (must be shared "anyone with link can view").
VITE_SHEET_ID=1Pp6RhsR96FzGfB3MV--CYj7Idja2-iyF7BNPhJ9Md_A

# Access password for the dashboard gate.
# NOTE: this is a light, client-side gate only (the value ships in the bundle).
# It keeps casual viewers out; it is NOT a substitute for real auth. See README.
VITE_DASH_PASSWORD=inquiries
